/**
 * @dss/core — 사내 시스템들이 **함께 쓰는** 서버 쪽 조각.
 *
 * 지금 들어 있는 것은 Drizzle 스키마 한 벌이다. A/S 관리 시스템과
 * PO/내자 시스템이 **같은 DB(dss_as)** 를 보기 때문에, 표 정의가
 * 두 벌이면 컴파일러가 어긋남을 잡아 주지 못한다. 그래서 한 벌만 둔다.
 *
 * 🔴 이 묶음은 **마이그레이션을 갖지 않는다.** `drizzle/` 과
 *    `drizzle.config.ts` 는 A/S 관리 시스템이 계속 소유한다.
 *    칸을 더하거나 고치는 일은 언제나 A/S 에서 한다(설계서 E-2절).
 *
 * 🔴 이 묶음은 **DB 에 접속하지 않는다.** 연결 · 환경변수 · 질의는
 *    가져다 쓰는 사이트가 갖는다. 여기 있는 것은 표의 모양뿐이다.
 *
 * 가져다 쓰는 방법은 README.md 를 볼 것.
 */
export * from "./schema";
